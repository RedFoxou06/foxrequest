# FoxRequest

**FoxRequest** est un client HTTP minimaliste et ultra-léger conçu pour tester des APIs rapidement. C'est mon alternative "mini-Postman" sans aucune dépendance, intégrée à l'écosystème FoxHub.

## 🌐 Utilisation
L'outil est accessible directement en ligne, sans rien installer :
👉 **[foxrequest.redfoxou.dev](https://foxrequest.redfoxou.dev)**

## 🚀 Pourquoi cet outil ?
Le but est de pouvoir débugger des endpoints ou tester des réponses JSON instantanément depuis n'importe quel navigateur. Pas de logiciel lourd à lancer, juste une interface efficace qui va droit au but.

## 🛠️ Fonctionnalités
* **Testeur d'API complet :** Support des méthodes `GET`, `POST`, `PUT`, `PATCH` et `DELETE`.
* **Vanilla JS :** Développé sans framework (Pure HTML/CSS/JS) pour une légèreté maximale.
* **Historique Local :** Sauvegarde tes dernières requêtes sur ton navigateur via `LocalStorage`.
* **Formatage JSON :** Les réponses sont automatiquement mises en forme pour être lisibles.

## 📂 Installation (pour développement)
Si tu souhaites l'exécuter localement :
1. Clone le dépôt.
2. Ouvre le fichier `index.html` dans ton navigateur.